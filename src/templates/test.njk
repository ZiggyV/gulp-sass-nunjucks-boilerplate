{% extends "layouts/default.njk" %}

{# set variables #}
{% set title = 'Test' %}
{% set title_pretty = 'Test page' %}

{# import macros #}
{% import "macros/macro-formField.njk" as field %}
{% import "macros/macro-grid.njk" as grid %}

{% block content %}
    <section id="contact">
        <div class="wrapper">
            <form action="" class="cf">
                <p>With Nunjucks you can <span class="bold">easily create</span> macros. They allow you to define <span class="bold">reusable chunks of content</span> that you can use freely accross your project. As you can see, the input fields and card grid below are <span class="bold">generated by macros</span>.</p>
                {# {{ field.input("username", "Username") }} #}
                {{ field.input("email", "Email address") }}
                {# {{ field.textarea("information") }} #}
                {{ field.submit("Subscribe") }}
            </form>
        </div>
    </section>

    <section id="cards" class="pt-0">
        <div class="wrapper wrapper--big">
            <h2 class="mb-md">16:9</h2>
            <div class="card__container">
                {% for card in cards %}
                    {{ grid.gridItem(card.title, card.image, card.alt, card.excerpt) }}
                {% endfor %}
            </div>
        </div>
    </section>
{% endblock %} 
